{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Radium PCs Companion desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "log:default"
  ]
}
